# Copy to `.env` or `.env.local` for local dev. Set the same in Vercel → Settings → Environment Variables.
# No trailing slash.
NEXT_PUBLIC_SITE_URL=https://www.ipdftools.com

# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
